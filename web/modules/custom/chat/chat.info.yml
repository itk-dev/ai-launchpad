name: "Chat blocks"
description: 'Frontend block to chat with LLMs.'
type: module
core_version_requirement: ^10
dependencies:
  - block:block
  - llm_services:llm_services
twig:
  paths:
    - templates
